describe("borrowBook", () => {
  it("borrows a book if it is available", () => {});
  it("does not borrow a book if it is unavailable", () => {});
});

describe("returnBook", () => {
  it("borrows a book if it is available", () => {});
  it("does not borrow a book if it is unavailable", () => {});
});